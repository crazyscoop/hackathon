name: api-trigger

on:
  repository_dispatch:
    types: do-something
    inputs:
      name:
        description: 'Person to greet'
        required: true
        default: 'Himanshu'
      home:
        description: 'location'
        required: false
        default: 'Nuclei'
  workflow_dispatch:
    inputs:
      name:
        description: 'Person to greet'
        required: true
        default: 'Himanshu'
      home:
        description: 'location'
        required: false
        default: 'Nuclei'

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Hello $!"
        echo "- in $!"     
    - run: |
        echo Did this work
    
